#include <stdio.h>
#include <math.h>

int main(void)
{
    double a = 6.21;
    double aSquared = pow(a, 2.0);

    printf("\n%.2lf square is %.2lf\n\n", a, aSquared);
    return 0;
}

// Function declaration /usr/include
// Function definition /usr/lib

